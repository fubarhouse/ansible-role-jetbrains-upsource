---

# ---
# Role created from instruction set found on https://boinst.wordpress.com/2014/04/30/install-and-update-teamcity-on-ubuntu/
# ---
#

- hosts: localhost

  vars_files:
    - ../defaults/main.yml
  pre_tasks:
    - include: ../tasks/tasks-initial.yml
  roles:
    - role: ../roles/geerlingguy.java/
      become: yes
      become_user: root
      when: "ansible_os_family == 'Debian'"
    - role: ../../fubarhouse.teamcity/
