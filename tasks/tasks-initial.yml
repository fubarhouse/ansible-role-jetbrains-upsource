---

- name: "Setup | Update apt cache"
  become: yes
  become_user: "root"
  apt:
    update_cache: yes
    cache_valid_time: 86400

- name: "Setup | Install Dependencies"
  become: yes
  become_user: root
  apt:
    name: "{{ item }}"
    state: installed
  with_items:
    - unzip