# Teamcity Apache configuration

<VirtualHost *:80>
    ServerName teamcity.loc.gbuild.net
    ProxyRequests off
    Keepalive On
    <Proxy *>
        Order deny,allow
        Allow from all
    </Proxy>
    <Location />
        ProxyPass http://localhost:8111/
        ProxyPassReverse http://localhost:8111/
    </Location>
    <Directory "/opt/teamcity">
      Options Indexes FollowSymLinks MultiViews
      Order allow,deny
      Allow from all
      AllowOverride All
      Require all granted
    </Directory>
</VirtualHost>
